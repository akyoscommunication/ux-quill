{% block quill_widget %}
    {% apply spaceless %}
        <div {{ stimulus_target('quill', 'editor') }}></div>
        <input {{ stimulus_target('quill', 'output') }} type="hidden" {{ block('widget_attributes') }} value="{{ value }}" />
    {% endapply %}
{% endblock %}
